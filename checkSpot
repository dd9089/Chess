//this is just a method because i do not know what class it should go in but in the implamentation here it is implied that it has access to 
//both player classes so that probably means it is in the board class

public Piece checkSpot(int x, int y)
{
	ArrayList(Piece) 1 = Player1.getPieces();
	
	for(int i = 0; i < 1.size(); i++)
	{
		if(1.get(i).getX == x && 1.get(i).getY == y)
		{
			return 1.get(i);
		}
	}
}
